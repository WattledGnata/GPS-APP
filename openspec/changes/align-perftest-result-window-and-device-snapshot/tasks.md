## 1. 领域窗口与结果计算

- [x] 1.1 新增 `PerformanceResultWindow` 及边界索引/精确时间偏移模型，并修改模型构造测试
- [x] 1.2 提取最后一个完整有效窗口的纯函数，新增“低速前段 + 失败尝试 + 最终 0–100”测试
- [x] 1.3 让 `CalculateResultUseCase` 从同一最终窗口计算耗时、距离、分段和 G 摘要，新增窗口外 G 尖峰排除测试

## 2. 原始遥测映射与读取

- [x] 2.1 对齐性能测试预触发、触发和运行帧的内存/binary 写入顺序及 GPS 相对时间，新增 sample 顺序与单调 delta 测试
- [x] 2.2 扩展 binary reader 按持久化窗口索引/偏移切片、边界插值和时间归零，新增 38 秒原始数据切出约 8 秒窗口测试
- [x] 2.3 为无窗口元数据的旧成绩增加只读重识别和失败降级测试

## 3. 持久化与设备快照

- [x] 3.1 扩展 `TestRecordEntity` 和 repository 映射以保存窗口元数据及测试开始设备快照，并修改映射测试
- [x] 3.2 在 `startTest` 冻结“别名 → 真实名 → 地址”设备快照，新增优先级与测试中断连不变测试
- [x] 3.3 将 Room 升级到 v11、加入 `migration10To11` 和连续迁移链，新增 SQL/链路/历史数据保留测试

## 4. 结果页一致性

- [x] 4.1 让详情页的速度曲线、G 曲线、平均/峰值 G 和分段统一消费最终窗口，并修改页面/派生逻辑测试
- [x] 4.2 修正 `SpeedChart` 真实最高速度和模板下限 Y 轴策略，新增 IQR 上界 13 但真实最大值 100 的回归测试
- [x] 4.3 为 G 曲线增加加速/制动方向量程、Y 轴数值和零线，新增 0.78G 单向量程测试

## 5. 验证与安装

- [x] 5.1 运行 `core:domain`、`core:data`、`feature:test` 单元测试和 Debug APK 构建，修复所有回归
- [x] 5.2 使用 `adb install -r` 安装到 vivo，验证旧 12:05 记录曲线兼容、设备名称和应用数据保留（覆盖安装成功且历史记录保留；旧记录曲线可只读重建，旧记录已保存的设备名按设计不追溯改写；新成绩设备快照仍按 5.3 留待下一次路测确认）
- [x] 5.3 记录仍需下一次新 0–100 路测验证的窗口字段、sample 数量一致及真机表现，不把静态/旧记录验证误报为完整路测通过
