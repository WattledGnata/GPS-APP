## 1. F1 governance 落地（先 land governance 条款）

- [ ] 1.1 在 `CLAUDE.md` "v3 高频盲点列表" 节末尾（盲点 #16 后）追加 #17 条款"实施期偏离 design 决策 MUST 暂停 apply 走 OpenSpec 修订流程"。条款体含（**L1 R1 P0-4 加严 actionable directive**）：(a) 触发场景（CC 在 /opsx:apply 中段发现某 task 实施需调整 design Decision）；(b) **actionable directive**："CC 主会话每次 apply 启动前 MUST grep 自检 round design 当前所有 Decision id 列表（如 `grep '^### Decision ' design.md`）；apply 期每完成 1 task 后 MUST 与 design Decision 比对（'该 task 是否仍按 Decision N 实施' / '实施期是否需要修订 Decision N'）；发生 drift 立即暂停 apply 写'实施期决策修订 §'到 design.md 或起 mini-proposal → L1 mini-review → resume apply"；(c) 修补 pattern（mini-proposal 模板：标题"实施期 Decision N 修订" + 触发原因 + 新决议 + 影响范围 + L1 mini-review 结论）；(d) 实战来源标记"phase1-hardening-w2-w3-w4-mimo-debt（W4 mimo 偷改 design Decision 1+2 教训）"；(e) **governance 局限透明声明**："F1 条款 enforce 路径仅靠 CC 自查；mimo 模式下不在 CC apply 流程内 → F1 dead；root-cause 修复路径在 OQ5（`/opsx:ff` skill 工具化 + commit hook drift 自动抓），需立 follow-up round `automate-design-drift-detection`"。**Done condition**：grep `CLAUDE.md` 出现 `#17` + `实施期偏离` + `MUST 暂停 apply` + `MUST grep 自检` + `每完成 1 task` + `actionable directive` + `governance 局限` 七个字面量

- [ ] 1.2 在 `CLAUDE.md` "L2 完成后 metrics.yaml" 节 yaml 例子加两个字段：(a) `design_decisions_diverged_during_apply: []` 注释"本 round apply 期是否触发 #17 条款；空 list 透明声明无 drift"；(b) `cross_round_field_drift_resolved: []` 注释"本 round 修复的跨 round 共享字段 drift；条目格式 'FieldClass.field (originRound→consumerRound)' 如 'LapTelemetrySample.flags (W1→W3)'"。**Done condition**：grep `CLAUDE.md` 出现两个字段名字面量；yaml 块形态正确（缩进对齐既有字段）

- [ ] 1.3 在 `CLAUDE.md` "Review v3 流程结束信号 + apply 启动条件" 节末尾追加 cross-reference 子条目"apply 期间发现 design 偏离 → 见 #17 条款（暂停 apply / OpenSpec 修订流程）"。**Done condition**：grep `CLAUDE.md` "Review v3 流程结束信号" 节内出现 "见 #17 条款" 字面量

- [ ] 1.4 (**L1 R2 OQ6 修订**) 在 `CLAUDE.md` 高频盲点 #16 "实战来源" 段尾追加 update note：「Phase 1 hardening round `phase1-hardening-w2-w3-w4-mimo-debt` 已修复 W3 LapAlignment.interpolate 路径（最近邻策略）；W4 binary writer 仍永久默认 0（deferred to Phase 2 — flags 信号源不在 RaceChrono BLE 协议中，需 Phase 2 加用户标记事件链路）；本 round 沉淀完整 producer/consumer 表见本 round design.md Decision 6」。**Done condition**：grep CLAUDE.md #16 实战来源段出现 "phase1-hardening-w2-w3-w4-mimo-debt" + "deferred to Phase 2" 字面量；MUST 在 #16 block 内（不超出 #16 边界）

## 2. A 类 W2 spec hardening（5 项）

- [ ] 2.1 (A1) 改本地主区 W2 archive `proposal.md` 关于 D1/D5 conflate 的措辞：找 proposal.md 中 mimo 写错的 "D1 决策 = A vs D" 描述（如 §"What Changes" 或 §"Why" 内出现 "D1=D" 字样的段落），改为正确措辞 "D1 = 类型契约依赖（决议=D，与 W1 编译可独立）"+ "D5 = TrackPolylineMap 底图策略（决议=A，user 拍板）"。文件路径：`/Users/wattledgnata/traeProjects/gps-app/openspec/changes/archive/2026-05-04-chart-and-map-components/proposal.md`（不进 git，本地修订即可）。**Done condition**：grep proposal.md 不再出现"D1=A vs D"误字 + 出现"D1 类型契约依赖"+ "D5 ... user 拍板 = A" 字面量

- [ ] 2.2 (A2) 改 `feature/test/src/test/java/com/blazepush/feature/test/ui/components/MockTelemetry.kt` `mockSingleLap` 函数体：找 `intervalMs` 计算行（mimo 实施大约 line 24-26，用 `60_000 / 99 = 606` 整除写法），替换为 `val elapsedMs = (i.toLong() * lapDurationMs) / (n - 1).coerceAtLeast(1)` 公式，确保 `sample[n-1].elapsedMsInLap == lapDurationMs` 严格等于。**Done condition**：grep MockTelemetry.kt 出现 `(i.toLong() * lapDurationMs) / (n - 1).coerceAtLeast(1)` 字面量；ContractTest 重跑 sample[99].elapsedMsInLap == 60_000L 断言通过

- [ ] 2.3 (A3) 改 `feature/test/src/test/java/com/blazepush/feature/test/ui/components/MockTelemetry.kt` `mockMultiLap` 函数签名 + `FakeLapTelemetry` 类型说明：函数返回类型 MUST 显式声明为 `List<FakeLapTelemetry>`（不是 `List<LapTelemetry>`），并在文件顶端注释加 KDoc "FakeLapTelemetry 是 src/test source set test-only 类型；W1 round 合回后 follow-up round wire-mock-telemetry-to-w1-real-classes 切换到正式类型"。**Done condition**：grep MockTelemetry.kt 函数签名 `fun mockMultiLap(): List<FakeLapTelemetry>` + KDoc 含"test-only"

- [ ] 2.4 (A4) 改 `feature/test/src/main/java/com/blazepush/feature/test/ui/components/AccelTimeChart.kt`：抽 `internal fun computeAccelSegments(samples: List<LapTelemetrySample>): List<IntRange>` 纯函数（按 design Decision 3 落在同文件），逻辑：从 sample 列表按 `accelerationG nullable` 拆分 IntRange list（连续非 null 段的 startIdx..endIdx）；空列表 / 全 null → emptyList()；全非 null → [0..size-1]。Composable 体内 path 渲染 MUST 改为 segments.forEach { range -> drawSegment(range) } 形式。**Done condition**：grep AccelTimeChart.kt 出现 `internal fun computeAccelSegments` 字面量 + Composable 体内出现 `computeAccelSegments(samples)` 调用 + 单元测试 5 case（全有值 / 部分 null 中段 / 全 null / 前段 null / 后段 null）全绿

- [ ] 2.5 (A5) 加 `feature/test/src/test/java/com/blazepush/feature/test/ui/components/AccelTimeChartContractTest.kt` 5 case："computeAccelSegments 全有值返回 [0..N-1]" / "中段 null 返回 [(0..M-1), (M+gap..N-1)]" / "前段 null 返回 [(M..N-1)]" / "后段 null 返回 [(0..M-1)]" / "多段交替返回 [(0..a), (b..c), (d..N-1)]"。每 case 用 mock LapTelemetrySample list 构造（手动构造或 mockSingleLap + 后处理 map { it.copy(accelerationG = null) }）。**Done condition**：5 个 @Test 方法新增 + ./gradlew :feature:test:testDebugUnitTest --tests "*AccelTimeChartContractTest*" 全绿

## 3. C 类 W2 silent bug 修复（4 项）

- [ ] 3.1 (C1，**L1 R1 P0-1 修订 + L1 R2 P0-R2-1 加严**) 改 `feature/test/src/main/java/com/blazepush/feature/test/ui/components/SpeedTimeChart.kt`：现状 `line 151` 已有 `if (coords.size >= 2)` 守卫 → n=1 chart line **不**渲染；现状 `line 161-167` cursor 渲染分支**未守卫** → n=1 + cursorAbsoluteTs match 时 `coords[0].x = elapsedMsInLap × canvasWidth / 1L` 远超 canvas，cursor line silent canvas 外（drawLine 仍调，GPU draw call 触发）；现状 `line 128-148` 触摸路径在 n=1 时 lapDurationMs=1L → 仍触发 `onCursorChange(samples[0].absoluteTsMs)`。修订动作（**MUST 走 (a) 路径**，不允许仅走 (b)）：(a) 在 Composable 体内 Box block 最早位置（`Box(modifier) {` 之后 5 行内）加 early-return `if (samples.isEmpty() || samples.size == 1) { 渲染占位文字 "NO DATA"; return@Box }` —— **一次性 cover 三条路径**（chart line + cursor line drawLine + 触摸 callback）；(b) 仅在 (a) 之外加保险，不可替代 (a)：在两个 `detectDragGestures` / `detectTapGestures` block 内分别加 `if (samples.size <= 1) return@detectXXX` 守卫保险层。**Done condition**：(1) verify `SpeedTimeChart.kt:151` `if (coords.size >= 2)` 守卫已存在（grep 命中）；(2) **L1 R2 P0-R2-1 关键 grep gate**：Composable 体内 Box block 起始（`Box(modifier) {` 之后 5 行内）grep 出现 `if (samples.isEmpty() || samples.size == 1)` 或 `if (samples.size <= 1)` 字面量（命中 = 1，**MUST 在 cursor 渲染分支 line 161-167 之前**）；(3) `computeChartCoordinates` 在 size <= 1 返回 emptyList 行为存在

- [ ] 3.2 (C1 测试，**L1 R1 P0-1 修订 + L1 R2 P0-R2-1 加 case 3 cursor 渲染**) 加 `feature/test/src/test/java/com/blazepush/feature/test/ui/components/SpeedTimeChartContractTest.kt` 3 case：(1) "n=1 走占位 不渲染 line"：构造 `samples = listOf(LapTelemetrySample(... elapsedMsInLap = 5000L ...))` 调用 `computeChartCoordinates` 断言返回 emptyList；(2) "n=1 触摸 0 callback"：用计数器 mock `onCursorChange = { invocationCount++ }`，模拟 drag/tap → 断言 `invocationCount == 0`；(3) **L1 R2 P0-R2-1 加 case "n=1 + cursorAbsoluteTs match 0 cursor line drawLine"**：构造 `samples = listOf(LapTelemetrySample(absoluteTsMs=100_000L, elapsedMsInLap=30_000L, ...))` + 外部 `cursorAbsoluteTs = 100_000L`（匹配 samples[0]），mock DrawScope 计数 drawLine 调用，断言 `cursor 线 drawLine 调用次数 == 0`（验证 (a) early-return 在 cursor 渲染分支之前生效）。**Done condition**：3 个新 @Test 方法 + 重跑 SpeedTimeChartContractTest 全绿 + 三计数器锁（chart line / 触摸 callback / cursor line drawLine 全为 0）

- [ ] 3.3 (C2) 改 `feature/test/src/test/java/com/blazepush/feature/test/ui/components/GrepGateTest.kt` §8.4 滑窗逻辑（mimo 实施大约 line 91-104，用 300 字符 `windowed(300)` 实现）：替换为 per-Text 块栈式匹配 — (a) 用 regex `Text\\(` 找起始位置；(b) 从起始位置 paren depth-counted 扫描直到 depth=0 找闭合 `)`；(c) 在该 [start, end] 区间内 grep `maxLines` + `overflow` 字面量；(d) 全部 Text 块都验证一遍。需引入辅助函数 `findTextCallBlocks(source: String): List<IntRange>` 实现 paren balance。**Done condition**：grep GrepGateTest.kt §8.4 内出现 `parenDepth` / `balanceCount` / `findTextCallBlocks` 字面量；MUST NOT 出现 `windowed(300)` / `windowed(200)` 字面量；4 组件源文件 §8.4 验证全绿（裸 Text 都含 maxLines + Ellipsis）

- [ ] 3.4 (C3) 改 `feature/test/src/test/java/com/blazepush/feature/test/ui/components/AccelTimeChartContractTest.kt` partial-null contract：找当前仅 `assertEquals(10, coords.size)` 的 partial-null case（mimo 实施大约 line 60-71），重写为基于 `computeAccelSegments(samples)` 真断言 IntRange list（与 task 2.5 共享纯函数，但本 case 是行为断言而非纯函数 unit test，重点在生产 Composable 拼装路径用了纯函数 segment 数据）。**Done condition**：grep AccelTimeChartContractTest.kt §partial-null case 出现 `computeAccelSegments` 调用 + IntRange list 断言

- [ ] 3.5 (C4) 重 verify W2 design 性能 baseline：改本地主区 W2 archive `design.md` 关于"60 FPS 1500 sample 渲染时间 < 5ms"的 section，**两选一**：(A) 跑实际 SpeedTimeChart drawScope 微测（用 JUnit + 简单 timing 测量；不必 androidTest 跑真机），把 baseline 写入 design.md（含跑测设备 / kotlin/gradle 版本 / 实际数据）；(B) 透明声明 "无量化性能 baseline，由 Tier 2 真机首次组屏签收"。文件路径：`/Users/wattledgnata/traeProjects/gps-app/openspec/changes/archive/2026-05-04-chart-and-map-components/design.md`。**Done condition**：grep design.md 不再出现 "5ms" 假数据字样；出现真实测量数据 OR "无量化性能 baseline" 透明声明字面量

## 4. B 类 LapTelemetrySample.flags drift 修复（4 项，v3 高频盲点 #16 实战首例）

- [ ] 4.1 (B1) 改 `feature/test/src/test/java/com/blazepush/feature/test/ui/components/GrepGateTest.kt` §8.7（LapTelemetrySample 字段 grep gate）：当前锁 7 字段；本 round 加锁第 8 字段 `val flags: Int`（命中数恰好 1 次；命中 0 次 → W1 字段被错误删除 fail；命中 ≥ 2 次 → 重复声明 fail）。文件路径 grep 锚点：`core/domain/src/main/java/com/blazepush/core/domain/model/LapTelemetry.kt`。**Done condition**：grep GrepGateTest.kt §8.7 出现 `val flags: Int` 字面量验证；命中数断言"恰好 1 次"

- [ ] 4.2 (B2，**L1 R1 P0-3 修订 + L1 R2 P1-R2-5 加 grep gate 实施 done condition**) 改 `core/domain/src/main/java/com/blazepush/core/domain/usecase/LapAlignment.kt`：现状 line 179-202 `interpolate(s0, s1, alpha, lapStartWallClock)` 函数变量名是 `s0 / s1`（不是 spec 数学符号 `s_k / s_{k+1}`），构造 `LapTelemetrySample` 在 line 193-201 `0 行 flags 字段` → 默认值 0 哨兵。修订：(a) 在 line 189 `bearingDeg = if (alpha < 0.5) s0.bearingDeg else s1.bearingDeg` 后追加 `val flags = if (alpha < 0.5) s0.flags else s1.flags`；(b) 在 line 193-201 LapTelemetrySample 构造体内追加 `flags = flags,` 字段；(c) 在 `resampleByGridFallback`（empty 圈 fallback 路径）函数内 LapTelemetrySample 构造时强制 `flags = 0`（与 design Decision 2 一致：empty 圈无源 sample 强制 0；非 empty 圈走最近邻）。**Done condition** 四锚点验证（防"等价三元表达式 + flags"宽松规则开后门）：(1) Regex `flags\s*=\s*if\s*\(\s*alpha\s*<\s*0\.5\s*\)\s*s0\.flags\s*else\s*s1\.flags` 命中数 = 1（interpolate 路径；命中 0 = 实施未落地 fail）；(2) Regex `flags\s*=\s*0` 命中数 = 1（仅 resampleByGridFallback 内；命中 ≥ 2 = interpolate 路径误用默认 0 fail）；(3) 全文 grep `flags = if` 命中数 = 1（防"双 if 嵌套"等隐式回退）；(4) **L1 R2 P1-R2-5 加 clamp 路径 grep gate**：扫 LapAlignment.kt findSampleAtDistance MUST 出现 `return samples[0]` 字面量 = 1 + `return samples[n - 1]` 字面量 = 1 + 0 命中 `samples[0].copy(flags = 0)` + 0 命中 `samples[n - 1].copy(flags = 0)`（防止重构 clamp 路径破坏 flags 保留）

- [ ] 4.3 (B3，**L1 R2 P0-R2-2 同步加 s0/s1 caveat**) 改本地主区 W3 archive `design.md` Decision 6（已合回时 follow-up 字段插值表）+ `specs/lap-comparison-alignment/spec.md` R6 字段插值表：(a) 把 `flags` 字段加进表，最近邻策略 + rationale "v3 高频盲点 #16 实战首例 by phase1-hardening-w2-w3-w4-mimo-debt"；(b) **同步加全局 caveat（与本 round spec.md 字段表前 global caveat 一致）**："本 design / spec 的 normative 描述用数学符号 `s_k / s_{k+1}`；实际生产代码 `LapAlignment.kt:179-202 interpolate(s0, s1, ...)` 函数变量名是 `s0 / s1`；apply 期 grep gate 字面量 MUST 用 s0/s1"。文件路径：`/Users/wattledgnata/traeProjects/gps-app/openspec/changes/archive/2026-05-04-lap-comparison-time-align/design.md` + 同目录 specs/lap-comparison-alignment/spec.md。本地修订（md 不进 git）。**Done condition**：grep design.md + spec.md 出现 `flags 最近邻` 字面量 + 引用 phase1-hardening-w2-w3-w4-mimo-debt round name + grep 出现 "实际生产代码 ... s0 / s1" / "grep gate 字面量 MUST 用 s0/s1" 字面量（与本 round spec.md global caveat 一致）

- [ ] 4.4 (B4，**L1 R1 P1-1 加 sub-G4/G5 + L1 R2 P1-R2-5 加 G5 grep gate done condition**) 加 `core/domain/src/test/java/com/blazepush/core/domain/usecase/LapAlignmentTest.kt` case G "flags 重采样最近邻" + 5 个 sub-scenario：(G1) α<0.5 取 s0.flags / (G2) α≥0.5 取 s1.flags 含值 0 / (G3) 重复距离区间取最小 index sample 的 flags / (**G4** clamp 路径 `d* < d_0` 直接 return samples[0] 含 flags，验证当前 LapAlignment.kt:155-156 `return samples[0]` / `return samples[n - 1]` 两条路径天然保留 flags) / (**G5** 精确命中重复距离区间起点 `d* == d_k` 直接 return samples[kMin] 含 flags，验证 LapAlignment.kt:160-164 精确命中分支 `return samples[kMin]`）。每 sub-scenario 用 mock laps 构造 flags=1 / flags=0 sample，调用 `LapAlignment.alignByDistance`，断言重采样后 grid sample.flags 严格等于源 sample.flags（**MUST NOT** 默认 0 哨兵）。函数命名：`caseG1_flagsAlphaSmallTakesS0` / `caseG2_flagsAlphaLargeTakesS1` / `caseG3_flagsDuplicateDistanceMinIndex` / `caseG4_flagsClampToFirstOrLast` / `caseG5_flagsExactMatchPreserveOriginal`。**Done condition**：(1) 5 个新 @Test 方法 + ./gradlew :core:domain:test --tests "*LapAlignmentTest*" 全绿；(2) **L1 R2 P1-R2-5 加 G5 grep gate**：扫 LapAlignment.kt findSampleAtDistance 精确命中分支 MUST 出现 `return samples[kMin]` 字面量（命中 = 1）+ 0 命中 `samples[kMin].copy(flags = 0)`（防止重构精确命中路径破坏 flags 保留）

## 5. D 类 W3 trivial（3 项）

- [ ] 5.1 (D1) 改 `core/domain/src/main/java/com/blazepush/core/domain/usecase/LapAlignment.kt` `resampleByGridFallback` 函数签名：删除死参数 `fallbackRefSamples: List<LapTelemetrySample>` + 删除 `@Suppress("UNUSED_PARAMETER")` 注解；同步删除该函数所有 caller 处的实参。**Done condition**：grep LapAlignment.kt 不再出现 `fallbackRefSamples` 字面量 + 不再出现 `@Suppress("UNUSED_PARAMETER")` 字面量；编译通过

- [ ] 5.2 (D2) 加 `core/domain/src/test/java/com/blazepush/core/domain/usecase/LapAlignmentTest.kt` case H "bearingDeg 跨 360° 边界最近邻"：构造 mock laps 含 `s_k.bearingDeg = 359f` + `s_{k+1}.bearingDeg = 1f`（跨 0/360 边界），断言 (a) α=0.3 grid 点 bearingDeg == 359f / (b) α=0.7 grid 点 bearingDeg == 1f / (c) 反向 case bearingDeg 不平均到 180f（防 mimo 漏测的边界 case）。**Done condition**：1 个新 @Test 方法 + 全绿

- [ ] 5.3 (D3) 加 `core/domain/src/test/java/com/blazepush/core/domain/usecase/LapAlignmentTest.kt` case I "elapsedMsInLap 浮点边界 deterministic"：构造 mock laps 累计距离 100.0000001m vs 100.0m vs 99.9999999m（±1e-9 浮点误差），调用 `gridIndexFor(100.0)` 三次断言返回值 deterministic 相同（在 step=5m 下都返回 20）；不出现摇摆。**Done condition**：1 个新 @Test 方法 + 全绿

## 6. E 类 W4 hotfix 后 P1（5 项）

- [ ] 6.1 (E1) 加 `feature/test/src/test/java/com/blazepush/feature/test/usecase/LapFilterIntegrationTest.kt` case "cleaned.timestamp == raw.timestamp"：构造 GpsData with timestamp = T，喂 `gpsDataFilter.process(it)` 得 filteredData，按 hotfix B 后契约构造 `cleaned = gpsData.copy(latitude=filteredData.latitude, longitude=..., speed=..., bearing=...)`，断言 `cleaned.timestamp == T`（不依赖 filteredData.timestamp）+ `cleaned.isTimeSynced == gpsData.isTimeSynced`。**Done condition**：1 个新 @Test 方法 + 重跑 LapFilterIntegrationTest 全绿

- [ ] 6.2 (E2) 改 `feature/test/src/main/java/com/blazepush/feature/test/usecase/LapLiveStateDeriver.kt:159` 注释：当前注释字面值"≥3"（mimo 把阈值降回 1 后没同步注释），改为"≥1"或更精确的"countInWindow >= LAP_INVALIDATED_DEBOUNCE_MIN_COUNT (本 round 后 = 1)"。**Done condition**：grep LapLiveStateDeriver.kt:155-165 范围内不再出现"≥3"或"countInWindow >= 3"字面量；出现"≥1"或常量名引用

- [ ] 6.3 (E3) 改本地主区 W4 archive `metrics.yaml` 补 placeholder 字段：`actual_days: 1.0` + comment "mimo 实施约 0.3d + L2 hostile review 0.4d + hotfix B 0.3d 整体闭环" / `review_rounds_l1: 0` + comment "mimo 跳过 L1 review" / `review_rounds_l2: 1` + comment "hotfix B 触发的 hostile L2 review，2026-05-05" / `review_findings_l1: []` + comment "mimo 跳过；属于 governance violation" / `review_findings_l2:` 列表填充 4 P0 + 5 P1（见 task context E 类描述）。文件路径：`/Users/wattledgnata/traeProjects/gps-app/openspec/changes/archive/2026-05-05-wire-laptime-to-gps-filter/metrics.yaml`（**进 git**，本 round 归档时一并 commit）。**Done condition**：grep metrics.yaml 不再出现 `null` 或 `# TODO` 字面量；review_findings_l2 长度 ≥ 9（4 P0 + 5 P1）

- [ ] 6.4 (E4) 加 `feature/test/src/test/java/com/blazepush/feature/test/usecase/LapFilterIntegrationTest.kt` case "binary sample 4 字段写 cleaned"：构造 GpsData + filteredData with 不同的 lat/lon/speed/bearing，按 hotfix B 后契约构造 cleaned，喂 bridgeGpsToLapTiming（lap mode 激活），读 binary sample 断言 binary 字段 == cleaned 字段（lat/lon/speedKmh/bearingDeg），timestamp 派生由 sessionStartTs 而非 raw.timestamp。可能需要 mock TelemetryRepository 或集成测试 setup（与既有 case 复用）。**Done condition**：1 个新 @Test 方法 + 全绿；test 内含 4 字段 binary 字面量断言

- [ ] 6.5 (E5，**L1 R1 P0-2 修订**) 在本地主区 W4 archive `tasks.md` 末尾**新增 §11 "归档后状态修订"**（W4 archive tasks.md 当前最后是 §10 follow-up backlog，本 round 紧接 §10 之后新增 §11；mirror W2 archive tasks.md §12 同名"归档后状态修订"命名约定，因 W4 §10 之后无 §11，编号沿用 §11；不是"修订" §12 — W4 archive 当前不存在 §12 章节）。新增 §11 内容含：(a) hotfix B 记录"2026-05-05 P0-1 hotfix B 修回 4 字段（commit 待 push）" + (b) W4_DIAG log 后续 strip 计划"真机 verify pass 后 CC 主会话独立 strip" + (c) tasks 1-10 勾选状态明示（参 W2 archive §12 模式）。文件路径：`/Users/wattledgnata/traeProjects/gps-app/openspec/changes/archive/2026-05-05-wire-laptime-to-gps-filter/tasks.md`（不进 git，本地修订）。**Done condition**：grep tasks.md 出现 `## 11. 归档后状态修订` 章节标题 + "hotfix B" + "W4_DIAG" + "真机 verify pass 后" 字面量；MUST NOT 出现"修订 §12" / "## 12." 字面量

## 7. 测试 gate（apply 期 + L2 review 前必跑）

- [ ] 7.1 跑 `./gradlew :feature:test:testDebugUnitTest`，含本 round 新增 case（A4/A5/B4/D2/D3/E1/E4/C1）+ 既有 case；全绿才放行。如有 fail 须暂停 apply 修复。**Done condition**：gradle exit code 0 + test report 0 fail

- [ ] 7.2 跑 `./gradlew :core:domain:test`（注意：core/domain 是 kotlin.jvm 模块，用 :test 而非 :testDebugUnitTest，参 W3 review_findings_l2 P1-3 教训），含本 round 新增 LapAlignmentTest case G/H/I + 既有 case；全绿才放行。**Done condition**：gradle exit code 0 + test report 0 fail

- [ ] 7.3 跑 `./gradlew :feature:test:testDebugUnitTest --tests "*GrepGateTest*"`，含本 round 修订的 §8.4 per-Text 块栈式匹配 + §8.7 flags 字段验证；全绿才放行。**Done condition**：GrepGateTest 全绿

- [x] 7.4 ~~真机视觉验证~~ — **2026-05-05 user 拍板取消**：本 round 22 项中 19 项视觉 0 影响（数据 / 测试 / governance），2 项渲染逻辑等价（A4 抽纯函数 / C3 加锁），仅 C1 SpeedTimeChart n=1 守卫有视觉行为变化但 n=1 是生产几乎不触发的边界 case（lap session 第一帧立即停止才触发）。本 round 走"纯单测覆盖 + governance plateau → 直接 push"路径。C1 n=1 边界 case 由 Tier 2 启动 round 真机首次组屏时一并 verify（与 Tier 2 OQ1 性能 baseline 同类活，避免重复跑）。W4 hotfix B 真机视觉验证是独立任务（属 W4 round scope，不属本 round）。

## 8. L2 review + 归档（CC 自闭环 + user 拍板 push）

- [ ] 8.1 调 general-purpose Opus 子 agent 跑 L2 adversarial review（CLAUDE.md "L1/L2 子 agent 调用" 节模板）：传 `model: "opus"` + prompt 用 `docs/templates/adversarial-review-prompt.md` + 占位符填本 round name / capability list / 复杂度 large / 重点 verify F1 governance 落地 + B 类 flags drift 真 propagate + C1 silent canvas 守卫真断言 + C2 GrepGateTest §8.4 paren balance 算法实现。**写 review trail** 到 `openspec/changes/phase1-hardening-w2-w3-w4-mimo-debt/review-l2-opus.md`。**Done condition**：review-l2-opus.md 文件存在 + 含 P0/P1/P2 findings 列表 + adversarial 收尾段

- [ ] 8.2 消化 L2 review findings：所有 P0/P1 修订到位 → 进入 8.3；如 L2 发现新 P0/P1 → 加追加轮 review（最多 2 轮 L2，超过则告知 CC 主会话回到 user 拍板）。**Done condition**：L2 plateau 信号清晰（无新 P0/P1 + memo 同步全过）

- [ ] 8.3 (**L1 R2 P1-R2-2 修订 cross_round_field_drift_resolved 字段值**) 写本 round `metrics.yaml`：`estimated_days: 1.0` / `actual_days: <实际>` / `review_rounds_l1: <实际>` / `review_rounds_l2: <实际>` / `review_findings_l1` + `review_findings_l2` 列表 / `divergence_reason` / `phase: "Phase 1"` / `model_apply: "opus"` / **`design_decisions_diverged_during_apply`**: list（如本 round apply 期未 trigger #17 条款则填 `[]` 透明声明） / **`cross_round_field_drift_resolved`**: `["LapTelemetrySample.flags: W1 producer (binary reader) + W2 grep gate consumer (this round B1) + W3 LapAlignment consumer (this round B2/B4); W4 binary writer permanent default 0 (unfixed, deferred to Phase 2 — flags signal source not yet defined in BLE GPS protocol)"]`（v3 #16 实战首例锚点 + W4 残漏透明声明）。文件路径：`openspec/changes/phase1-hardening-w2-w3-w4-mimo-debt/metrics.yaml`。**Done condition**：metrics.yaml 含 governance 两个新字段 + cross_round_field_drift_resolved 字段值含 "W4 binary writer permanent default 0" 字面量 + 完整字段无 placeholder

- [ ] 8.4 W2/W3/W4 archive 工件 sync（cp 本 round 修订到主区 archive 目录）：本 round 在 worktree 内修订的本地 archive md（A1 W2 proposal / B3 W3 design+spec / C4 W2 design / E5 W4 tasks）— 这些文件在 worktree 内本来就是直接改主区 absolute path 的，sync 是 noop；本步骤主要 verify 主区 archive 目录文件内容已更新。**Done condition**：grep 主区 archive 文件出现本 round 修订的关键字面量（如 W3 design.md "flags 最近邻" / W4 tasks.md "hotfix B"）

- [ ] 8.5 [需 user 拍板] ff-only 合回 `feature/track-tech-v2`：等 user 真机 verify pass + 拍板 push 顺序后，从主区 `git checkout feature/track-tech-v2 && git merge --ff-only feature/phase1-hardening-w2-w3-w4-mimo-debt`。多 round 同时就绪时 user 决定顺序（本 round vs W4 hotfix B vs 其他）。**Done condition**：user 明示授权 + ff-only 成功

- [ ] 8.6 [需 user 拍板] push：等 user 拍板后 `git push origin feature/track-tech-v2`。MUST 注意 kt-format-checker 对 push 历史逐条验证；如 reject 不允许 --no-verify，须修违规重 push。**Done condition**：user 明示授权 + push 成功 + kt-format-checker 通过

- [ ] 8.7 归档 + 清理 worktree：跑 `openspec archive phase1-hardening-w2-w3-w4-mimo-debt` 把工件 cp 到 archive；删 changes 原目录；archive commit（仅 metrics.yaml 进 git，其余 md 全 ignored 但本地保留）；最后 `git worktree remove .worktrees/phase1-hardening-w2-w3-w4-mimo-debt` 清理。**Done condition**：archive 目录 `2026-05-XX-phase1-hardening-w2-w3-w4-mimo-debt/` 含 metrics.yaml；worktree 目录已删

## 9. 跨 capability 牢固性自检（apply 期 fresh eyes）

- [ ] 9.1 grep 主区 archive 三个 round（W2/W3/W4） 工件 verify 本 round 修订的关键 P0/P1 都在 archive 文件中体现 — 即使 md 不进 git，本地 archive 也是 review trail 沉淀点。具体 grep：(a) W2 archive `proposal.md` 出现 D1/D5 修订；(b) W3 archive `design.md` 出现 flags 最近邻 + phase1-hardening-w2-w3-w4-mimo-debt round name；(c) W4 archive `metrics.yaml` 出现完整 review_findings_l2 + actual_days。**Done condition**：3 个 grep 命中

- [ ] 9.2 grep 本 round 工件 verify F1 条款是否真 demo：本 round apply 期间是否触发 #17 暂停 apply 走 OpenSpec 修订流程；如有 trigger → metrics.yaml `design_decisions_diverged_during_apply` 列出条目；无 trigger → 透明 declare `[]`。**Done condition**：metrics.yaml 该字段存在 + L2 review trail 含一段 governance 自检结论

## 10. Follow-up backlog（不在本 round 实施，沉淀到 archive）

- [ ] 10.1 follow-up `wire-mock-telemetry-to-w1-real-classes`（Tier 2 启动前）：W1 round 合回后把 W2 的 `FakeLapTelemetry` 切换到正式 `LapTelemetry` 类型；`MockTelemetry.kt` mockMultiLap 返回类型改 `List<LapTelemetry>`；同步 W2 spec scenario 。memo 沉淀：本 round tasks §10 backlog（不另立 deferred memo，因为切换是机械化操作）

- [ ] 10.2 follow-up W4_DIAG log strip（W4 真机 verify pass 后由 CC 主会话独立 round 或单 commit）：strip `feature/test/src/main/java/com/blazepush/feature/test/usecase/GateCrossingDetector.kt:117` + `LapLiveStateDeriver.kt:166` + `viewmodel/TestSessionViewModel.kt:358` 三处 W4_DIAG 临时 log。**触发条件**：user 真机 verify W4 hotfix B 后明示 strip

- [ ] 10.3 follow-up `redesign-realtime-delta-projection-search`（独立路径，已与 user 决策 Tier 2 设计修复）：DELTA tile prevMatchedIdx 跨 lap 未重置；本 round 不 scope，只在 backlog 标记，避免 review 期混淆。

- [ ] 10.4 retrospective trigger（每 5 round 一次，CLAUDE.md 约定）：本 round 是 Phase 1 第 5 个归档 round（W1 + W2 + W3 + W4 + 本 round），归档后应跑一次 retrospective —— 聚合 W1-W4 mimo 模式与本 round opus 模式的 metrics 对比（actual_days / review findings 数 / divergence_reason 模式），沉淀到 docs/retrospective/phase-1-mimo-vs-opus.md。memo 路径：`docs/retrospective/phase-1-mimo-vs-opus.md`（本地不进 git，CC 主会话拉起独立 session）

- [ ] 10.5 (**apply 期 §7 测试 gate 发现 — W4 mimo 缺漏 test 同步 P1**) `TestSessionViewModelTrackLapTest.lapDebugMode_secondStartFinishClosesLapEvenWhenSectorChainIsIncomplete` (line 197-217) baseline HEAD 4326e11 已 fail（expected: 1 但实际 0）。**根因**：W4 commit `e2f4417` 把 `LAP_INVALIDATED_DEBOUNCE_MIN_COUNT 3→1`，导致 lap debug mode 中"sector chain 不完整时 1 帧 invalidating reason 立即触发 banner → lap 失效 → completedLaps=0"，与原 test 期望"sector chain 不完整时 lap 仍闭环 completedLaps=1"行为变化。**W4 mimo 缺漏**：mimo 实施期未同步更新此 existing test 的 expected。**处置**：本 round 不顺手修（属 lap business decision，需 user 拍板"sector chain 不完整时 lap 是否仍闭环"），独立立项 follow-up round `fix-lap-debug-mode-sector-chain-test-after-min-count-1`（small 复杂度）。**透明声明**：本 round metrics.yaml `review_findings_l2` 含此 baseline 已 fail（v3 高频盲点 #14 fake DAO 漏 abstract 同款）

- [ ] 10.6 (**apply 期 mistake-2 沉淀**) 21 项 apply 代码改动全部在主区 working tree（feature/track-tech-v2 分支），不在 worktree feature/phase1-hardening-... 分支。**原因**：CC 主会话 apply 期用 absolute path Edit 改主区物理 .kt 文件。**user 决策**：A 路径接受现状（apply 改动 + hotfix B 同主区，commit + push 一起拍板）。**worktree 内仅含 4 工件 + R1/R2/R3 review trail + metrics.yaml**（本 round 自身 metadata），不含代码改动；归档时本 round metrics.yaml 进 git（其他 md ignored）。**经验沉淀**：未来 round MUST 在 worktree cwd 执行 Edit；CLAUDE.md 看板 §5 加 worktree workflow caveat（不在本 round scope，独立 sed memo）

## 11. v3 #16 worked example（本 round 自身 = 实战首例）

- [ ] 11.1 (**L1 R2 OQ6 修订**) 在本 round 归档时 metrics.yaml `cross_round_field_drift_resolved` 字段值见 task 8.3（含 W4 binary writer 残漏透明声明）；同时：(a) 在 W3 archive `tasks.md §10 follow-up backlog` 补 note "v3 高频盲点 #16 实战首例已修复 by phase1-hardening-w2-w3-w4-mimo-debt round（本 round B2/B4 修 LapAlignment 最近邻策略 + clamp/精确命中路径锁）" + W3 review trail link；(b) 在 W1 archive `design.md` 加 retroactive section "消费 LapTelemetrySample.flags 字段的已合回 round 列表（按 #16 normative）"，列 W2/W3/W4 角色（参 本 round design.md Decision 6 表）。**Done condition**：(1) W3 archive tasks.md 含 phase1-hardening 引用；(2) W1 archive design.md 含"消费此字段的已合回 round 列表"section（含 W2/W3/W4）；(3) 本 round metrics.yaml 含 cross_round_field_drift_resolved 字段
