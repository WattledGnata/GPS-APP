estimated_days: 0.5
actual_days: 0.4        # workflow 第一版（全屏垫底）+ user 路测反馈 + 子 agent 返工（横滑 pager）+ 真机验
complexity: "medium"
review_mode: "road-test-first"
review_rounds_l1: 0
review_rounds_l2: 0
codex_l1_findings: []
codex_l2_findings: []
review_findings_l1: []
review_findings_l2: []
divergence_reason: "第一版全屏垫底预览被 user 路测否（不喜欢 + 担忧驾驶时持续预览功耗/发热）→ 返工 HorizontalPager 横滑独立预览页（相机仅 settledPage==1 绑，驾驶页 0 不开相机=省电省热）"
phase: "Phase 2"
model_apply: "opus"
design_decisions_diverged_during_apply: []
cross_round_field_drift_resolved: []
accelerated_escalation: null
# --- road-test-first 落地（含一次路测反馈返工）---
# 第一版（workflow b14410b）：CameraX 预览全屏垫底 LapLiveScreen + opt-in toggle + 懒请求权限，不录制。真机预览出图 OK。
# user 反馈：① 不喜欢全屏垫底（要小窗或横滑独立屏）② 担忧驾驶时一直预览的功耗/发热。
# 返工（3960a8a）：HorizontalPager 2 页——页0 纯 HUD（驾驶，不开相机）/ 页1 相机预览（对取景）。
#   核心省电：CameraPreview 仅 settledPage==1 时进 composition 绑相机；回页0→onDispose unbindAll 释放（驾驶时相机不开）。
#   权限 settledPage==1 懒请求（复用 RequiredCameraPermissions + PermissionRequestOutcome）；三态（无相机/需权限+授权按钮/预览）。
#   去 toggle + 全屏垫底；横滑提示 dot+chevron。仍只 Preview use-case 不录制（VideoCapture=0，round3）。
# 验证：CameraPreviewContractTest 9 绿（页0无预览/settledPage gating/无 record token/权限复用）；feature:test 375/0fail；
#   :app build SUCCESSFUL；真机 vivo V2405A：横滑到页1出预览、回页0相机释放、权限三态、用户签收。
# 复用 round1 core:camera（camera-view/lifecycle 提 api）+ RequiredCameraPermissions；重组安全 if/else 无 early-return（M2 教训）。
