estimated_days: 0.7
actual_days: 0.25
complexity: "medium"    # Room schema migration 强制升级例外 3
review_mode: "road-test-first"
review_rounds_l1: 0
review_rounds_l2: 0
codex_l1_findings: []   # road-test-first 模式去 Codex
codex_l2_findings: []
review_findings_l1: []
review_findings_l2: []
divergence_reason: "apply 期 3 透明声明:(1) 工件初稿漏盘 video-storage-cleanup spec 锁的'重录删旧'行为,编译 gate 抓出后 inline 修订 proposal Modified Capabilities + delta spec + 既有测试语义反转;(2) deleteSession 行删除从'FK CASCADE 兜底'改显式 deleteBySessionId(fake 假绿自审发现);(3) 构造 callsite 实测 9 文件(预估 8)+ 断言连锁实测 4 处(预估 3)"
phase: "视频管线债务批 ②a(三子 round 之一)"
model_apply: "opus"
design_decisions_diverged_during_apply: []  # D1-D6 无 drift;删旧废止是 D3 既定方案,spec 对齐属 #17 caveat 的 spec drift 路径
cross_round_field_drift_resolved: []
accelerated_escalation: null
# 日志锚点摘要(road-test-first MANDATORY):
#   - Log.d tag=VideoSegment @ attachVideoToSession(segment appended: index/playable/durationMs/双写)
#   - Log.d tag=VideoSegment @ deleteSession/deleteSessionVideo(cascade removed N segments)
#   - FileLogger @ CameraRecordingEngine 两调用方既有锚点扩展(playable/durationMs 实参落盘)
# 真机攒批 MUST:(1) v8 旧包升装 v9:开库不崩 + video_segments 表存在 + 存量迁出 index=0 行
#               (2) 录一段→停→再录:子表两行 + 旧字段=第二段 + 第一段文件仍在 + 回放行为不变
# Follow-up:②b video-segment-recording-rotation(N=3 圈/段 user 已拍板) → ②c video-segment-playback-export
